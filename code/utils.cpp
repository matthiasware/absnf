#include <stdio.h>
namespace utils
{
	template <typename T>
	void fillMatrix(T matrix, int size, char const* options = "none")
	{

	}
	template <typename T>
	void fillVector(T vector, int size)
	{

	}
	void pp()
	{
		printf("UTILS\n");
	}
}